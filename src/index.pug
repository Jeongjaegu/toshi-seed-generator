doctype html
html(class='no-js', lang='en')
  head
    meta(charset='utf-8')
    meta(http-equiv='X-UA-Compatible', content='IE=edge,chrome=1')
    meta(name='viewport', content='width=device-width, initial-scale=1.0')

    script(type='text/javascript', src='qr.js')

    title Token Seed Generator

  body
    .container
      h1 Token Seed Generator
      p See #[a(href="http://developers.tokenbrowser.com/docs/creating-a-token-app") Creating a Token App] for usage.
      button#generate Generate
      h3 TOKEN_APP_ID
      input(type='text', id='address')
      h3 TOKEN_APP_PAYMENT_ADDRESS
      input(type='text', id='payment_address')
      h3 TOKEN_APP_SEED
      input(type='text', id='code')
      p You can also paste a seed here ^ to generate the App ID
      br
      h4 Optional: Generate a QR Code
      p You can generate a QR code for your bot by inputting its TOKEN_APP_USERNAME below. This code can be scanned using the Token mobile client to add your bot as a contact.
      h3 TOKEN_APP_USERNAME
      input(type='text', id='username')
      #qrcode
